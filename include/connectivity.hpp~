// connectivity.hpp

#pragma once

#include "trgl6_icos.hpp"
#include <vector>
#include <array>

void compute_nbe(const std::vector<std::array<int, 6>>&, std::vector<std::array<int, 3>>&);
void compute_ne(const std::vector<std::array<int, 6>>&, const std::vector<std::array<double, 3>>&, std::vector<std::vector<int>>&);
void compute_ne(Mesh& mesh);
